CREATE TABLE `currency` (

    `currency_record_record_pk` int NOT NULL AUTO_INCREMENT PRIMARY KEY,
    `code` varchar (10),
    `num` varchar(10),
    `number_of_digits` INT,
    `currency` varchar(200)

)ENGINE=InnoDB DEFAULT CHARSET=UTF8;

